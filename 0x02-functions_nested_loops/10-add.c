#include "main.h"
/**
 * add -entry point
 * Description; the function to add two numbers
 * @a: the first input number
 * @b: The second input number
 * Return: c the addition of the two numbers
 */
int add(int a, int b)
{
	int c = a + b;

	return (c);
}
