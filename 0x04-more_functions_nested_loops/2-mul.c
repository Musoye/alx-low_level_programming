#include "main.h"

/**
 * mul - entry point for multiplication of two numbers
 * @a: the first number
 * @b: the second number
 * Return: the multiplird result
 */

int mul(int a, int b)
{
	int num = a * b;

	return (num);
}
